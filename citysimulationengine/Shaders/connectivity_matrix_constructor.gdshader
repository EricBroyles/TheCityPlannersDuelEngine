shader_type canvas_item;
render_mode unshaded;

/*
Takes in a terrain_type matrix
assumes the thing I am drawing to has the same size as the tt matrix
also takes in a array of ints from 0 to 8, if value of tt matches one of those, it gets labeled
*/



void fragment() {
	// Called for every pixel the material is visible on.
}

